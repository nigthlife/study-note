内聚耦合：
      功能内聚：完成一个单一功能，各个部分协同工作，缺一不可
      顺序内聚：处理元素相关，而且必须顺序执行
      通信内聚：所有处理元素集中在一个数据结构的区域上
      过程内聚：处理元素相关，而且必须按特定的次序执行
      瞬时内聚：所包含的任务必须同时在同一时间内执行
      逻辑内聚：
      偶然内聚：

	非直接耦合：
	数据耦合：
	标记耦合：
	控制耦合：
	外部耦合：
	公共耦合：
	内容耦合：
 