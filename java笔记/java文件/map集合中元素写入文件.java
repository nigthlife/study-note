





                    int s = (random.nextInt(10));
                    j++;
                    str += s;
                    System.out.println();
                card[i] = str;
                if(i % 3 ==0){
                int j = 0;
                str += "139";
                str = "";
                System.out.print((i+1)+"."+card[i]+"\t");
                while (j  < 8){
                }
                }
            for (int i = 0; i < 9; i++) {
            Map.Entry<String, String> next = iterator.next();
            return card;
            String str = "";
            stringBuffer.append(next.getKey()+" : "+next.getValue()).append(line);
            }
        Iterator<Map.Entry<String, String>> iterator = entries.iterator();  //再获得一个迭代器对象
        map.put("1","王昌龄");
        map.put("2","归属感");
        map.put("3","身高多");
        map.put("4","割发代");
        map.put("5","狂欢购");
        Map<String,String> map = new HashMap<>();
        public String[] RandomNumber(String[] card){
        Set<Map.Entry<String, String>> entries = map.entrySet();        //set集合
        String line = System.getProperty("line.separator");     //获得一个分隔符
        StringBuffer stringBuffer = new StringBuffer(); //用于连接map中的元素值
        System.out.println(stringBuffer);
        while (iterator.hasNext()){
        }
        }
    public static void main(String[] args) {
public class Test_2 {